<Interface>
 <config>
    <!-- CallControl session/application protocol.
         The value must match the module base name: cc_server resolves
         "<CC-proto>.so" and the symbol "<CC-proto>_proto_bind".
         jsonrpc_cc -> jsonrpc_cc.so / jsonrpc_cc_proto_bind -->
    <param name="CC-proto" value="jsonrpc_cc" />
    <!-- Network transport: udp, tcp, tls, sctp -->
    <param name="proto" value="tls" />
    <!-- IP version: IPv4, IPv6 -->
    <param name="ip-version" value="IPv4" />
    <param name="ip" value="" />
    <param name="port" value="9092" />
    <!-- TLS server credentials (PEM). Required when proto == "tls".
         'cert' is the server certificate chain, 'key' its private key.
         A single certificate is used process-wide across TLS interfaces. -->
    <param name="cert" value="/usr/local/RateEngine/config/certs/server.crt" />
    <param name="key"  value="/usr/local/RateEngine/config/certs/server.key" />
    <!-- Mutual TLS (optional, default off). verify-client=yes rejects any client
         that does not present a certificate chaining to the CA bundle 'ca'. -->
    <param name="verify-client" value="no" />
    <param name="ca" value="/usr/local/RateEngine/config/certs/ca.crt" />
 </config>
</Interface>
