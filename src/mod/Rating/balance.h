#ifndef BALANCE_H
#define BALANCE_H

#define SQL_TMP_BAL "create temp table tmp_bal as select * from balance;"
#define SQL_TMP_FREE_BAL "create temp table tmp_free_bal as select * from free_billsec_balance;"

#endif
