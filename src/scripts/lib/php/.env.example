# RE7 database connection for the shared PHP library.
# Copy to .env (git-ignored) and adjust. Never commit real credentials.
RE7_DB_HOST=127.0.0.1
RE7_DB_PORT=5432
RE7_DB_NAME=rate_engine
RE7_DB_USER=rt_admin
RE7_DB_PASS=change_me
